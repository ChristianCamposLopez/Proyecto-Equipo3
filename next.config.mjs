// next.config.mjs
// Eliminar redirección automática a dashboard para navegar manualmente
const config = {
  // no redirects defined; el usuario decide adónde ir desde / o un enlace en la UI
};

export default config;
